
# python execute -n networkFile (optional) -i interferenceModelAndInfo (optional) -e experimentFile -d demandFile (optional) -f jammingDefFile (optional)

# read in parameters from command line

# read in experiment file using XML

# run david's script -n networkFile -i (optional) interferenceModelAndInfo (optional)

# run jamming generator script -f jammingDefFile

# run the math model, print some results to file, print some results to database

